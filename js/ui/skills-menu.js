import {SKILL_GROUPS,SKILL_CAP} from '../data/skills.js';
export class SkillsMenu{constructor(game){this.game=game;this.dialog=document.getElementById('skills-dialog');this.list=document.getElementById('skills-list');document.getElementById('open-skills').addEventListener('click',()=>{this.render();this.dialog.showModal()})}render(){const book=this.game.player.skills;document.getElementById('skill-total').textContent=`${book.total().toFixed(1)} / ${SKILL_CAP}`;this.list.innerHTML='';for(const[group,label]of Object.entries(SKILL_GROUPS)){const title=document.createElement('h3');title.textContent=label;title.style.gridColumn='1/-1';title.style.margin='8px 0 0';this.list.append(title);book.skills.filter(s=>s.group===group).forEach(s=>{const row=document.createElement('div');row.className='skill-row';row.innerHTML=`<div>${s.name}<small>Máximo actual: ${s.cap}</small></div><strong>${s.value.toFixed(1)}</strong>`;this.list.append(row)})}}
